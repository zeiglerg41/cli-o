#!/bin/bash
# Wrapper script to run claude-clone in Docker

# Build image if it doesn't exist
if [[ "$(docker images -q claude-clone:latest 2> /dev/null)" == "" ]]; then
    echo "Building claude-clone image..."
    docker-compose build
fi

# Run the container
docker-compose run --rm claude-clone "$@"
