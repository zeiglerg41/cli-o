#!/bin/bash
# Wrapper script to run CLIO in Docker

# Build image if it doesn't exist
if [[ "$(docker images -q clio:latest 2> /dev/null)" == "" ]]; then
    echo "Building CLIO image..."
    docker-compose build
fi

# Run the container
docker-compose run --rm clio "$@"
